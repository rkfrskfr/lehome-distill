# 주간 보고 — LeHome 옷 접기 파이프라인 (2026-09-08, 월)

작성: 박지환. 대상: 연구실 선배·지도교수. 세부 수치는 `RESULTS.md`(자동 생성), 전체 배경은 `PROJECT_REPORT.md`.

## 1. 한 줄 요약

- 우승자 대형 모델(2.83B) → 실물 탑재용 소형 모델(51.7M) 파이프라인 구축 완료. 고친 환경에서 **원본 85.8% / 소형 최고 76.7%** (긴팔 12벌 × 10회; 소형은 시연 674회 학습 모델, Unseen 70%).
- 이번 주 핵심 사건: 환경 버그 2건(판정 기준점, 옷 낙하 높이) 발견·수정 (9/5). 수정 후 원본 모델 성적이 공식 리더보드(74.5%)와 정합. 이전 수치는 전부 "수정 전 환경" 기준.
- 실물 SO-101 연동 코드 준비 완료(로봇 없이 프로토콜 검증 통과). 월요일 실물 테스트 예정.

## 2. 이번 주 진행 내용

### 2-1. 판정기 버그 (가장 중요)

- 증상: 옷 한 벌(Seen_8)이 원본 모델·소형 모델 모두 성공률 0%. "성공 조건이 서로 모순인 옷"으로 알고 평가에서 제외해 왔음.
- 원인: 물리 엔진이 옷 메시를 입자로 변환할 때 좌표가 위로 23cm 이동됨. 이식한 판정기는 이를 보정하지 않고 "가장 가까운 입자"를 기준점으로 잡아 **모든 옷의 기준점이 실제 위치에서 어긋남**. Seen_8은 어깨 기준점 거리가 4.6cm(기준 10.8cm)로 애초에 통과 불가.
- 확인: 12벌 전부에서 "메시 중복 점 수 == 입자 변환 후 사라진 점 수"임을 확인 → 변환 규칙(같은 좌표 점은 첫 번째만 유지) 역산으로 기준점 인덱스를 정확히 복원. 용접된 옷 3벌에서 복원 후 거리가 메시 기준과 0.5cm 이내 일치.
- 공식 코드 대조: 공식 판정기는 입자 배열을 인덱스 그대로 사용(공식 환경은 용접이 없다는 전제). 수정된 판정기가 공식 의미와 일치.
- 영향: 9/5 17:00 이전 성공률(원본 45.8~50%, 소형 47.5% 등)은 **수정 전 판정** 기준. 수정 후 원본·소형·변형 모델을 같은 조건에서 재측정 중(§3).
- 수정 후 판정은 훨씬 엄격함: 원본 모델이 공식 리더보드에서는 긴팔 74.5%(20벌×10판)인데, 우리 복제 환경에서는 수정 후 판정으로 20%대(중간 집계). 실패의 대부분이 "5개 조건 중 4개 통과, 마지막 거리가 기준을 수 mm 초과" 유형 → 복제 환경의 물리 차이(적분 주기 등, `HANDOFF.md` §7-1)가 주원인으로 추정. 절대값 비교는 공식 환경이 아닌 "복제 환경 기준"으로 명시.

### 2-1b. 두 번째 버그: 옷 낙하 높이 (9/5 20:35)

- 판정기 버그의 "+23cm" 출처를 추적하다 발견. 물리 엔진 초기화 시 옷이 원점(테이블 내부)에서 생성되어 테이블에 밀려난 좌표(위로 23cm, 7° 기울기)를 "옷의 기준 자세"로 저장하고 있었음.
- 결과: 매 시도마다 옷 중심을 0.86m(테이블 위 34cm)에서 떨어뜨림. 공식 환경은 옷 하단이 테이블 1cm 위에서 시작(거의 얹어 놓는 수준). 즉 우리 환경은 공식보다 훨씬 구겨진 상태에서 시작 → 공식 74.5% 대비 성공률이 낮았던 주요 원인 후보.
- 수정 후: 옷 하단 3.5cm 위에서 시작, 안착 후 옷이 더 넓게 펴짐. 9/5 20:35 이전의 모든 수집 데이터·평가는 "높은 낙하" 조건.
- **검증 결과**: 원본 모델(우승자 모델)을 수정된 환경·판정기로 재측정 → 12벌 120회 중 103회 성공(**85.8%**, Seen 86% / Unseen 85%, Seen_8 10/10). 수정 전 22%, 공식 리더보드 긴팔 74.5%. 즉 복제 환경이 이제 공식 환경과 같은 난이도. 이전에 "원본 45.8~50%, 소형 47.5%" 로 보고한 수치는 잘못된(더 어려운) 환경에서 나온 값.

### 2-2. 데이터·학습 실험 (수정 전 판정·높은 낙하 기준, 상대 비교만 유효)

| 실험 | 결과 | 해석 |
|---|---|---|
| 행동 묶음 100스텝 실행 → 5스텝 실행 후 재계획 | 31% → 66% (32판 부분 평가) | 폐루프 주기가 지배 변수. 재학습 없이 2배 |
| 시연 700회 전량 학습 | 30.8% | 랜덤화 없는 초기 200회 혼입이 역효과 추정 |
| 대형화(파라미터 1.5배) | 44.2% | 267회 모델(47.5%) 못 넘음 |
| 랜덤화 일관 데이터 505회 | 측정 중(수정 후 판정) | |

### 2-3. 자동화·인프라

- 무인 실험 루프(수집→변환→학습→평가→판단) 구축. 주말 동안 자동 실행 중.
- 멀티에이전트 코드 감사로 결함 49건 확정·40여 건 수정 (`AUDIT.md`).
- GPU 과적재 사고(학습+평가 2개+원본 모델 서버 동시 → 전 작업 10배 지연) → 평가 동시 실행 1개 원칙으로 체인 재편.
- GitHub 공개: https://github.com/rkfrskfr/lehome-distill

### 2-4. 실물 연동 준비

- 소형 모델 서버 → 실물 양팔 브리지(`31_real_robot_bridge.py`) 작성. 단위 규약(라디안↔도, 그리퍼 0~100) 명시, 안전장치(1회 최대 20° 이동, dry-run 기본).
- 로봇 없이 프로토콜 검증(`--selftest`) 통과: 관측 → 모델 → 12관절 명령 변환, CPU에서도 15ms/스텝.
- 월요일 순서: 통신 확인 → 홈 자세 이동(부호·오프셋 정합) → dry-run → 실구동.

## 3. 결과 (수정 후 판정) — 월요일 아침 `python compile_results.py` 로 갱신

(RESULTS.md 표를 여기에 붙임)

- 원본 모델 `teacher_v3` : **85.8%** (103/120; Seen 86% / Unseen 85%)
- 이전 최고 소형 모델(수정 전 데이터 267회로 학습) `final_n5_rep` : **58.3%** (70/120; Seen 63% / Unseen 35%) — 수정 전 환경 47.5% → 고친 환경에서 상승. 원본과 격차 27%p
- 증강 OFF `noaug_full_n5` : **55.0%** (66/120) — 같은 267회 데이터, 이미지 증강만 끔. 증강 ON(58.3%) 대비 -3%p, 시드 분산(±4%p) 이내라 증강 효과는 미확정
- **시연 674회(수정 전 505회 + 고친 환경 169회) 학습 모델 `combo_n5` : 76.7%** (92/120; Seen 78% / Unseen 70%) — **현재 최고 소형 모델**(실물 테스트·데모 기본값). 원본(85.8%)과 격차 9%p. 처음 보는 옷(Unseen)에서 가장 좋음
- 랜덤화 일관 데이터 505회 학습 모델 `cur_n5` : 75.8% (91/120) / 2차 평가(다른 시드 120회) 70.8% → **240회 기준 73.3%**. 수정 전 환경에서 만든 데이터인데도 고친 환경에서 잘 동작 → 시연 데이터 양(267→505→674회)이 성능을 좌우
- 같은 505회 모델의 40k 스텝 체크포인트 `cur40k_n5` : 70.0% (84/120) — 60k(75.8%)가 더 좋음. 마지막 체크포인트 사용이 손해가 아님을 확인
- 고친 환경 재수집 169회만으로 학습한 모델 `fix_n5` : 40.0% (48/120) — 데이터 양 부족. 시작 상태를 정확히 맞춘 적은 데이터보다, 많은 데이터가 더 중요하다는 근거
- 학습 시드 변경 `seed2_n5` : **62.5%** (75/120) — 같은 레시피·다른 시드. 58.3% 와의 차이 4%p 가 시드 분산 수준
- 평가 오차: 옷당 10회 × 12벌 = 120회 기준 95% 신뢰구간 약 ±8%p. 같은 모델을 시드만 바꿔 두 번 재면 5%p 차이(75.8 vs 70.8). 모델 간 3~5%p 차이는 유의하지 않음
- **고친 환경에서 재수집한 데이터로 새로 학습한 모델 `fix_n5` : __ %** (핵심 비교 대상)
- (수정 전 데이터 기반 실험 chunk30/cur2/cur240 은 낙하 높이 버그 발견으로 중단 — 잘못된 시작 상태의 데이터라 의미가 낮음)

## 4. 다음 주 계획

1. 실물 SO-101 구동 테스트 (월) — 674회 모델 그대로, 성공 여부보다 "움직임이 시뮬과 같은 방향·크기인지" 확인이 1차 목표
2. 시연 데이터 확대 — 데이터 양이 성능을 좌우한다는 결과(267→505→674회: 58→73→77%)에 따라 고친 환경에서 1,000회 이상 수집 후 재학습. 원본(85.8%)과의 9%p 격차 축소가 목표
3. 처음 보는 옷(Unseen) 일반화 — 옷 종류·질감 랜덤화 확대
4. 실물 카메라 3대 정렬(sim2real) — 우승자 논문 §sim2real 절차 이식

## 5. 질문 예상·답

- "판정기 버그면 그동안 수치는 다 무효?" → 상대 비교(설정 A vs B)는 유효, 절대값은 재측정 필요. 재측정 완료분은 §3.
- "공식 채점기와 같나?" → 공식 코드와 대조해 의미 일치 확인. 단 우리 환경(Windows Isaac Sim 5.1)은 입자 변환 시 중복 점을 용접해 인덱스 보정이 필요했음. 12벌 중 11벌은 보정 유무와 무관하게 같은 기준점이고, Seen_4 한 벌만 해석에 따라 달라짐(우리는 기하학적으로 의도된 어깨 점을 사용).
- "실물에서 바로 접히나?" → 아니오. 시뮬 전용 학습 모델이라 우선 동작 정합 확인. 실물 성공은 sim2real 단계 이후.
