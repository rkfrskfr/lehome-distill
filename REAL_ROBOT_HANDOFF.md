# 실물 SO-101 작업 인수인계 (2026-09-07)

**이 문서 하나만 읽고 실물 로봇 작업을 시작할 수 있도록 쓴 문서.** 시뮬레이션 대화방과 분리해서
실물 팔 작업만 여기서 진행한다. 시뮬레이션 개발·학습·평가는 다른 대화방에서 계속되므로,
이 방에서는 **시뮬레이션 실험을 건드리지 않는다** (금지 사항은 15절).

문서 안의 모든 명령은 **PowerShell** 기준이다. Git Bash 는 쓰지 말 것. `C:\...` 경로의 역슬래시와
`.venv\Scripts\activate` 가 모두 깨진다.

---

## 1. 이 방에서 할 일

시뮬레이션에서 학습을 마친 옷 접기 모델을 **실물 SO-101 양팔 로봇에 올려서 돌린다.**

이번 단계의 목표는 옷을 실제로 접는 것이 **아니다**. 아래 세 가지 확인이 목표다.

1. 모델이 낸 명령이 실물 팔까지 끊김 없이 전달되는가.
2. 팔이 시뮬레이션과 **같은 방향으로, 비슷한 크기로** 움직이는가.
3. 팔이 테이블·서로·자기 자신과 부딪히지 않는가.

옷이 안 접히는 것은 정상이다. 이유는 2절 마지막에 있다.

---

## 2. 프로젝트 배경 (모르는 사람 기준)

- **LeHome 챌린지**: ICRA 2026(로봇 분야 최대 학회) 공식 대회. 양팔 로봇으로 테이블 위 옷을 접는 과제.
  시뮬레이션 라운드와 실물 로봇 결선으로 나뉜다.
- **우승자**: 시뮬 라운드 1위(79.63%), 실물 결선 2위. 28억 파라미터 모델을 썼고 코드를 공개했다.
- **우리가 한 것**: 대회 환경을 윈도우 Isaac Sim 에 복제하고, 우승자 모델을 **시연 생성기**로 써서
  5,170만 파라미터짜리 작은 모델을 학습시켰다. 큰 모델은 실물에 얹기에 무겁고 느리기 때문이다.
- **성적** (긴팔 상의 12벌, 시뮬레이션 기준. 자세한 표는 16절)

| 모델 | 성공률 | 평가 횟수 |
|---|---|---|
| 우승자 대형 모델 (28억) | 85.8% | 120 |
| **우리 소형 모델 (5,170만)** | **77.1%** | 240 |

- **옷이 실물에서 바로 안 접히는 이유**: 이 모델은 시뮬레이션 화면만 보고 배웠다. 실물 카메라가 보는
  색·그림자·질감은 시뮬과 다르다. 이 차이를 메우는 작업(sim2real)이 다음 단계이고, 우승자도 여기에
  따로 일주일을 썼다.

---

## 3. 지금까지 확인된 것과 확인 안 된 것

**확인됨 (실제로 실행해서 통과)**
- 소형 모델이 관측 3장 + 관절 12개를 받아 관절 명령 12개를 내는 전 과정.
- 모델 파일만 복사하면 다른 컴퓨터에서 그대로 로드된다. 학습 데이터는 필요 없다.
- 관절 라디안 ↔ 실물 도(degree) 변환 코드가 12개 관절 이름을 모두 정확히 만들어 낸다.
- **계산 시간 실측** (2026-09-07, 데스크톱 Core Ultra 9 285K / RTX 5090)

| 실행 장치 | 신경망이 도는 스텝 | 나머지 스텝 | 실제 제어 주기 |
|---|---|---|---|
| GPU | 19밀리초 | 1밀리초 | 30Hz 유지 |
| CPU (24스레드) | **196밀리초** | 3밀리초 | 약 15Hz |
| CPU (4스레드) | 465밀리초 | 7밀리초 | 약 9Hz |

  모델은 한 번 계산해 5스텝치 명령을 만들어 두고 4스텝은 그걸 꺼내 쓴다. 그래서 5스텝에 한 번만 무겁다.
  **CPU 만 있으면 목표 30Hz 를 못 채우고 절반 정도로 돈다.** 팔이 학습 때보다 느리게 움직인다는 뜻이며,
  이번 단계(방향·크기 확인)에는 문제 없지만 나중에 실제로 접게 하려면 GPU 가 있는 편이 낫다.
  브리지가 실행 중 5초마다 실제 주기를 출력하니 확인할 것.

**확인 안 됨 (실물이 없어서 못 해본 것)**
- 실제 모터 통신. 포트 이름, 영점, 관절 방향 부호가 맞는지.
- 카메라 3대 동시 동작.
- 실물에서의 움직임 품질.

---

## 4. 컴퓨터 구성

| | 역할 | 비고 |
|---|---|---|
| 데스크톱 (RTX 5090) | 시뮬레이션·학습 전용 | **다른 대화방이 쓴다.** 이 방에서 GPU 작업 금지 |
| 노트북 등 다른 PC | 실물 로봇 연결 | 여기서 모델도 같이 돌린다 |

**모델과 로봇을 같은 컴퓨터에 두는 것이 권장 구성이다.** 나눠서 네트워크로 연결하면 매 스텝 사진 2.8MB 를
보내야 하고, 기가비트 유선에서도 왕복에 22밀리초가 들어가 33밀리초 예산의 대부분을 먹는다. 무선은 불가능하다.
(그래도 나눠야 하면 서버에 `--host`·`--allow-ip`, 브리지에 `--host` 옵션이 있다.)

노트북에 NVIDIA 그래픽카드가 있으면 그쪽이 훨씬 낫다(3절 표). 없으면 CPU 로 돌리되 주기가 절반으로
떨어지는 것을 감안한다.

### 옮길 폴더

데스크톱에 이미 만들어져 있다: `C:\Users\H\Desktop\lehome_portable` (242MB).
없거나 오래됐으면 데스크톱에서 다시 만든다.

```powershell
powershell -ExecutionPolicy Bypass -File C:\Users\H\Desktop\lehome-win\make_portable.ps1
```

**이 폴더를 노트북으로 통째로 복사한다.** 6절부터는 전부 노트북에서 하는 작업이다.

- USB 로 옮기는 것이 가장 간단하다. 242MB 라 아무 USB 나 된다.
- 데스크톱에서 USB 로 바로 만들려면 `-Out E:\lehome_portable` 처럼 경로를 준다 (드라이브 문자는 실제 값으로).
- 노트북에서는 `C:\lehome_portable` 처럼 경로가 짧은 곳에 두는 것을 권한다. 이 문서는 그 경로를 예로 쓴다.

폴더 안에는 모델, 실행 스크립트 2개, 문서 3개, 명령 모음, 영상 인식 부품 파일이 들어 있다 (13절).

---

## 5. 하드웨어 배치

시뮬레이션 배치를 그대로 흉내 낼수록 잘 맞는다. 시뮬 좌표(미터, 바닥 기준)는 다음과 같다.

| 항목 | 시뮬 값 | 실물로 옮기면 |
|---|---|---|
| 왼팔 베이스 | (-0.23, -0.25, **0.500**) | 두 팔 간격 **46cm**, 같은 선 위에 나란히 |
| 오른팔 베이스 | (+0.23, -0.25, **0.500**) | 두 팔이 같은 방향(옷 쪽)을 본다 |
| 옷 | 원점 부근 | 두 팔 앞 **25cm** |
| 테이블 상판 | 0.521 | **팔 베이스 기준면이 상판보다 2.1cm 아래.** 상판과 같은 높이로 맞추면 안 된다 |
| 위 카메라 | 약 (0, +0.19, 1.06) | 옷 기준 팔 **반대편** 약 20cm, 상판에서 **54cm 위**, 테이블을 내려다봄 |

**팔 베이스 높이가 가장 민감하다.** 시뮬에서 우승자 모델로 높이만 바꿔 측정한 값
(옷 4벌 × 15~20회, `bench_brittle_z*.csv`)

| 베이스 높이 | 0.490 | 0.495 | **0.500 (공식)** | 0.505 | 0.510 | 0.520 |
|---|---|---|---|---|---|---|
| 성공률 | 85% | 65% | **93%** | 67% | 55% | **7%** |

- 5mm 만 어긋나도 25~30%p 가 빠진다. 상판과 같은 높이(0.520)로 만들면 사실상 전멸이다.
- 단 이 측정은 2026-08-20 자료로, 9/5 에 고친 환경 버그 2건 이전 조건이다. 16절 수치와 직접 비교하지 말고
  **"수 mm 단위로 민감하다"는 방향만** 받아들일 것.
- 두 팔을 같은 판에 고정해 간격과 높이를 한 번에 정하는 것이 첫 관문이다.

**카메라**
- 손목 카메라 2대는 **각 팔의 그리퍼에 물리적으로 붙어야 한다.** 삼각대 고정 카메라로 대체 불가.
  모델은 "손이 움직이면 화면도 따라 움직인다"는 전제로 배웠다.
- 화각(가로): 위 카메라 약 67.2도, 손목 카메라 약 53.5도. 해상도는 셋 다 640×480.
  (`lehome_scene.py` 의 `CAMS` 초점거리·센서폭에서 계산한 값이다.)

정확한 각도를 맞추려면 데스크톱에서 시뮬 데모를 띄워 카메라 3대 화면을 직접 보고 비교하는 것이 가장 빠르다.
단 **다른 대화방이 GPU 를 쓰고 있지 않을 때만** 실행한다.

---

## 6. 노트북 소프트웨어 설치

파이썬 **3.12 이상**. 3.11 이하는 설치가 거부된다.

이식 폴더를 노트북에 복사해 둔 뒤, 그 폴더 안에서 가상환경을 만든다. 폴더 안에 만들어 두면
7절에서 재활성화가 한 줄로 끝난다.

```powershell
cd C:\lehome_portable
```

```powershell
py -3.12 -m venv .venv
```

```powershell
.venv\Scripts\activate
```

```powershell
pip install "lerobot[feetech]==0.6.1" opencv-python
```

- `[feetech]` 를 빼면 안 된다. 모터 통신 부품이 그 안에 있고, 없으면 팔 연결 순간 오류가 난다.
  이름이 그럴듯한 `[hardware]` 에는 정작 모터 부품이 없다.
- 그래픽카드가 없으면 용량이 작은 CPU 전용 파이토치를 먼저 깔아도 된다.
  `pip install torch --index-url https://download.pytorch.org/whl/cpu`
- 이식 폴더의 `torch_hub\resnet18-f37072fd.pth` 를 노트북의
  `%USERPROFILE%\.cache\torch\hub\checkpoints\` 로 옮긴다. 안 옮기면 첫 실행에서 인터넷으로 45MB 를 받는다.
- `activate` 가 ExecutionPolicy 에 막히면 `powershell -ExecutionPolicy Bypass` 로 새 창을 연다.

설치 확인

```powershell
python -c "import scservo_sdk, serial, cv2, torch; print('설치 OK')"
```

---

## 7. 실행 절차

**명령 창 두 개를 쓴다.** 7-4 의 모델 서버 창 하나, 7-5~7-9 의 브리지 창 하나.
**두 창 모두** 이식 폴더로 이동한 뒤 가상환경을 활성화해야 한다.

```powershell
cd C:\lehome_portable
```

```powershell
.venv\Scripts\activate
```

활성화를 잊으면 `lerobot-find-port` 를 못 찾거나 `ModuleNotFoundError: No module named 'lerobot'` 이 뜬다.

같은 폴더의 `REAL_ROBOT_RUNBOOK.md` 에 같은 절차가 더 자세히 있다.

### 7-0. 시작 전 확인 (처음 한 번)

| 항목 | 수량 | 확인 사항 |
|---|---|---|
| SO-101 팔 (follower) | 2 | **조립이 끝나 있어야 한다.** 관절 6개 |
| 팔 전원 어댑터 | 2 | **팔마다 별도.** USB 만 꽂으면 포트는 잡혀도 모터를 못 읽는다 |
| USB 데이터 케이블 | 2 | 충전 전용 케이블은 안 된다 |
| 카메라 3대 · 손목 거치 2개 | - | 규격·위치는 5절 |

연결 순서: 전원 어댑터 먼저 → USB 연결 → 아래 7-1.

### 7-1. 포트 찾기

```powershell
lerobot-find-port
```

안내대로 한 팔의 USB 를 뽑았다 꽂으면 그 팔의 포트 이름(COM5 등)이 나온다. 두 팔 각각 실행한다.
장치 관리자의 포트 항목에 아예 안 보이면 WCH 사의 `CH343SER` 드라이버를 설치한다
(윈도우 11 은 대체로 자동으로 잡힌다). 리눅스면 `sudo usermod -aG dialout $USER` 후 재로그인.

### 7-2. 영점 잡기 (팔마다 1회)

```powershell
lerobot-calibrate --robot.type=so101_follower --robot.port=COM5 --robot.id=lehome_bi_left
```

```powershell
lerobot-calibrate --robot.type=so101_follower --robot.port=COM6 --robot.id=lehome_bi_right
```

1. "가동 범위 중앙으로 옮기고 엔터" → 팔을 반쯤 접은 자세로 손으로 옮긴 뒤 엔터.
2. "손목회전 빼고 모든 관절을 끝에서 끝까지" → 관절 하나씩 최대로 돌린 뒤 엔터.

- 로봇 종류 이름은 `so101_follower` 다. 저장 폴더 이름이 `so_follower` 라 헷갈리기 쉽다.
- 이름을 정확히 `lehome_bi_left` / `lehome_bi_right` 로 해야 브리지가 찾는다.
- 저장 위치는 다음 두 곳이고, 브리지는 이 두 파일만 본다.
  ```
  %USERPROFILE%\.cache\huggingface\lerobot\calibration\robots\so_follower\lehome_bi_left.json
  %USERPROFILE%\.cache\huggingface\lerobot\calibration\robots\so_follower\lehome_bi_right.json
  ```
- **다른 팔의 영점 파일을 이름만 바꿔 쓰지 말 것.** 영점 값은 팔에 **연결하는 순간 파일에서 모터로 기록**된다.
  브리지가 연결할 때 "Press ENTER to use provided calibration file …" 이 뜨는데, 여기서 엔터를 누르면
  그 파일 값이 그대로 모터에 들어간다. 다른 팔의 값이면 오류 없이 **조용히 어긋난 각도**로 움직인다.

### 7-3. 카메라 번호 확인

```powershell
lerobot-find-cameras opencv
```

- 번호별로 사진 한 장씩을 **명령을 실행한 폴더 밑** `outputs\captured_images\` 에 저장한다.
- 출력의 `Camera #0` 은 목록 순서일 뿐이다. 쓸 번호는 그 아래 `Id:` 값이고 파일 이름의 번호와 같다.
- 사진 3장을 열어 **위 → 왼손목 → 오른손목** 이 각각 몇 번인지 확인하고 그 순서로 적는다.
  (예: 위=2, 왼손목=0, 오른손목=1 이면 적어 둘 값은 `2,0,1`)
- 이 도구와 브리지는 카메라를 여는 방식이 달라 번호가 어긋날 수 있다. 7-8 에서 실제 화면으로 다시 확인한다.

### 7-4. 모델 서버 켜기 (이 창은 켜 둔 채로)

```powershell
python 12_policy_server.py model --n-action-steps 5 --device cpu
```

- `model` 은 **반드시 첫 번째 인자**여야 한다. 스크립트가 첫 인자를 체크포인트 경로로 읽는다.
- `--n-action-steps 5` 는 **반드시 붙인다.** 기본값이면 카메라를 한 번 보고 3.3초를 눈 감고 움직인다.
  시뮬에서 이 옵션 하나로 성공률이 두 배 차이 났다.
- 그래픽카드가 있으면 `--device cpu` 를 빼면 자동으로 GPU 를 쓴다. 훨씬 빠르다 (3절 표).
- `--ensemble` 은 쓰지 말 것. 계산이 5배로 늘어 주기를 못 맞춘다.
- "체크포인트 로드 완료" 와 "대기 중" 이 뜨면 준비된 것이다.

### 7-5. 로봇 없이 통신 확인 (두 번째 창에서)

```powershell
python 31_real_robot_bridge.py --selftest
```

"12관절 명령 생성·변환 정상" 이 나오면 소프트웨어 쪽은 끝. 로봇도 카메라도 필요 없다.

### 7-6. 팔 통신 확인

```powershell
python 31_real_robot_bridge.py --check --left COM5 --right COM6
```

두 팔의 현재 각도가 도 단위로 나온다. 손으로 팔을 움직인 뒤 다시 실행해 숫자가 따라 변하는지 본다.
끝낼 때 토크를 푼다는 안내가 뜨고 엔터를 기다린다 (9절).

### 7-7. 홈 자세 이동 — 이번 작업의 핵심

```powershell
python 31_real_robot_bridge.py --home --left COM5 --right COM6
```

목표 각도를 먼저 보여 주고, 엔터를 누르면 5초에 걸쳐 천천히 이동한다.
**실물이 시뮬 시작 자세와 같은 모양이 되는지** 확인한다. 시뮬 홈 자세는 팔을 접어 몸쪽으로 모은 모양이다.
모양이 다르면 8절의 표를 고친다.

### 7-8. 명령만 출력 (팔은 안 움직임)

```powershell
python 31_real_robot_bridge.py --run --left COM5 --right COM6 --cams 0,1,2
```

- 실행 직후 각 카메라의 실제 해상도·형식이 출력된다. **`MJPG` 인지 확인**한다.
  `YUY2` 로 나오면 USB 대역폭이 모자라 카메라 3대가 함께 안 열린다. 서로 다른 USB 단자로 나눠 꽂는다.
- 5초마다 실제 제어 주기(Hz)가 출력된다. 25Hz 아래면 경고가 함께 나온다.
- 숫자가 급격히 튀지 않고 부드럽게 변하는지, 관절 범위를 벗어나지 않는지 본다.

### 7-9. 실제 구동

```powershell
python 31_real_robot_bridge.py --run --left COM5 --right COM6 --cams 0,1,2 --live
```

처음에는 옷 없이 빈 테이블에서 팔 궤적만 보는 것을 권한다. Ctrl+C 로 멈춘다.

---

## 8. 단위·좌표 규약 (실수가 가장 잦은 부분)

| 구간 | 단위·순서 |
|---|---|
| 모델 출력 | 12개 값, **라디안**, 순서 = [왼팔 6, 오른팔 6] |
| 관절 순서 | 어깨회전, 어깨들기, 팔꿈치, 손목굽힘, 손목회전, 그리퍼 |
| lerobot 실물 | 팔 5관절은 **도(degree)**, 그리퍼는 **0~100** |
| 목표 제어 주기 | 초당 30회 |

변환은 `31_real_robot_bridge.py` 위쪽 표가 담당한다.

```python
SIGN = {m: 1.0 for m in MOTORS}        # 방향이 반대인 관절만 -1.0
OFFSET_DEG = {m: 0.0 for m in MOTORS}  # 각도가 통째로 밀린 관절만 값 입력
GRIP_RAD_OPEN, GRIP_RAD_CLOSED = 0.7, -0.2
```

- 관절이 **반대로 돌면** 그 관절의 `SIGN` 을 -1.0 으로.
- 방향은 맞는데 **일정 각도 밀려 있으면** 그 차이를 `OFFSET_DEG` 에 넣는다.
- 그리퍼가 반대로 열고 닫으면 `GRIP_RAD_OPEN` 과 `GRIP_RAD_CLOSED` 를 서로 바꾼다.
  이 두 값은 수집 데이터(60판) 실측 범위(닫힘 -0.2~-0.1, 열림 최대 0.69)를 감싸도록 잡은 값이다.
- **한 관절씩 고치고 매번 `--home` 을 다시 실행하는 것이 가장 빠르다.**

시뮬 홈 자세 (참고, 라디안)
```
오른팔  [ 1.2363, -1.7135, 1.4979, 1.0534, -0.085, -0.01176]
왼팔    [-1.2363, -1.7135, 1.4979, 1.0534, -0.085, -0.01176]   # 어깨회전만 부호 반대
```

---

## 9. 안전

- 처음 전원을 넣을 때 팔 주변 1m 를 비운다. 전원 스위치 근처에 손을 두고, 이상하면 즉시 차단한다.
- **팔은 연결되는 순간부터 힘이 들어가 있고, 종료할 때 힘이 풀려 그 자리에서 떨어진다.**
  브리지는 끊기 직전에 "이제 토크를 풉니다" 안내를 띄우고 엔터를 기다린다.
  **그 전에 팔을 손으로 받치거나 테이블에 닿는 낮은 자세로 만들어 둘 것.**
- 특히 `--home` 직후가 위험하다. 자세를 눈으로 확인하려고 팔 밑에 손이나 머리를 넣지 말 것.
- 전원을 그냥 끄는 것도 같은 결과다. 팔이 떨어진다.
- 한 번에 움직이는 양은 제한되어 있다. 팔 5관절은 최대 20**도**, 그리퍼는 0~100 척도에서 20**칸**이다.
  같은 숫자가 두 단위에 그대로 적용된다.
- 더 조심하려면 `--max-rel 10`. 단 폐루프에서 그리퍼 여닫는 속도도 절반이 된다.
  `--home` 은 5초에 걸쳐 나눠 보내므로 이 제한에 걸리지 않는다.
- `--live` 없이 먼저 돌려 보는 습관을 들인다.

---

## 10. 알려진 함정

| 함정 | 증상 | 대처 |
|---|---|---|
| 가상환경 미활성화 | `lerobot-find-port` 없음, `ModuleNotFoundError` | 창마다 `.venv\Scripts\activate` |
| `--n-action-steps 5` 누락 | 팔이 3초씩 눈 감고 움직임 | 서버 옵션 확인 |
| CPU 로만 실행 | 주기가 10~15Hz 로 떨어짐 | 정상. GPU 가 있으면 `--device cpu` 를 뺀다 |
| 카메라 형식이 YUY2 | 카메라 일부가 안 열림 | USB 단자 분산 |
| 영점 파일 혼동 | 오류 없이 엉뚱한 각도 | 팔마다 새로 잡기 |
| 로봇 종류 이름 | `so_follower` 로 쓰면 실패 | `so101_follower` |
| 파이썬 3.11 이하 | 설치 자체가 거부됨 | 3.12 이상 |
| `[feetech]` 누락 | 팔 연결 순간 오류 | 재설치 |
| 인터넷 없는 환경 | 첫 실행에서 멈춤 | resnet18 파일 미리 복사 |
| 원격 분리 구성 | 주기가 크게 떨어짐 | 한 컴퓨터에서 전부 실행 |
| `--host 0.0.0.0` 단독 사용 | 보안 위험 | `--allow-ip` 로 상대 제한 |

---

## 11. 이번 작업의 성공 판정

아래 세 가지가 되면 성공이다. 옷이 접히는지는 보지 않는다.

1. 끊김 없이 계속 도는가 (주기는 CPU 면 10~15Hz 라도 정상).
2. 팔이 시뮬과 같은 방향·비슷한 크기로 움직이는가.
3. 충돌이 없는가.

기록해 두면 좋은 것: `--home` 결과 사진, `SIGN`/`OFFSET_DEG` 최종 값, 카메라 번호와 형식,
브리지가 출력한 평균 Hz, 이상 동작이 있었던 관절.

---

## 12. 다음 단계 (이번 주 이후)

1. **카메라 정렬**: 실물 카메라 3대를 시뮬과 같은 각도로 맞춘다. 시뮬 화면과 실물 화면을 겹쳐 보며 조정한다.
2. **실물 시연 수집**: 사람이 직접 조종해 접는 시연을 모아 모델을 실물 데이터로 미세 조정한다.
3. **시뮬 쪽 개선분 반영**: 다른 대화방에서 데이터를 늘려 모델을 다시 학습 중이다. 새 모델이 나오면
   `model\` 폴더만 교체하면 된다. 실행 방법은 같다.

---

## 13. 파일 지도

이식 폴더 `lehome_portable` 안

| 파일 | 역할 |
|---|---|
| `model\` | 학습된 소형 모델 (197MB). 정규화 값이 안에 들어 있어 학습 데이터 불필요 |
| `12_policy_server.py` | 모델 서버. 관측을 받아 관절 명령 12개를 돌려준다 |
| `31_real_robot_bridge.py` | 실물 브리지. 카메라·팔·서버를 잇는다. 단위 변환표가 여기 있다 |
| `REAL_ROBOT_HANDOFF.md` | 이 문서 |
| `REAL_ROBOT_RUNBOOK.md` | 단계별 실행 순서 (7절의 상세판) |
| `LAPTOP_SETUP.md` | 설치 안내 (6절의 상세판) |
| `COMMANDS.txt` | 복사해 쓰는 명령 모음 |
| `torch_hub\` | 영상 인식 부품 파일 (6절대로 옮길 것) |
| `calibration\` | 데스크톱의 옛 영점 파일. **참고용일 뿐 그대로 쓰지 말 것** (7-2) |

데스크톱 원본은 `C:\Users\H\Desktop\lehome-win` 이고, 시뮬 전체 배경은 `PROJECT_REPORT.md`,
실험 결과표는 `RESULTS.md` 에 있다.

---

## 14. 브리지 실행 모드와 옵션

```
--selftest   로봇·카메라 없이 서버와의 통신만 확인
--check      두 팔 연결 + 현재 각도 출력
--home       시뮬 홈 자세로 5초에 걸쳐 이동 (부호·오프셋 확인용)
--run        폐루프. 기본은 명령 출력만, --live 를 붙여야 실제로 움직인다
```

모드마다 읽는 옵션이 다르다. 이 스크립트는 `sys.argv` 를 직접 훑으므로
**그 모드가 읽지 않는 옵션을 붙여도 오류 없이 조용히 무시된다.** 설정이 먹은 줄로 착각하기 쉽다.

| 모드 | 읽는 옵션 |
|---|---|
| `--selftest` | `--host` `--port` |
| `--check` | `--left` `--right` `--max-rel` `--id` |
| `--home` | `--left` `--right` `--max-rel` `--id` |
| `--run` | 위 전부 + `--cams` `--host` `--port` `--timeout` `--live` |

---

## 15. 이 방에서 하지 말 것

- **데스크톱의 시뮬레이션 프로세스를 끄지 말 것.** 다른 대화방이 학습·평가를 자동으로 돌린다.
  대상은 `kit.exe`(Isaac Sim), `lerobot-train`, `12_policy_server.py`, `15_winner_relay.py`,
  그리고 마커 파일을 기다렸다가 다음 단계를 띄우는 체인 스크립트(`*_markers.log` 를 감시하는 PowerShell)다.
  포트는 8000(우승자 모델 서버, WSL), 8767(중계기), 8766·8768·8769(소형 모델 평가 서버)를 쓴다.
  현재 상태는 아래로 확인한다.

```powershell
Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match '(kit\.exe|lerobot-train|12_policy_server|15_winner_relay|_chain|_eval)' } | Select-Object ProcessId, Name, CommandLine
```

- 데스크톱에서 GPU 를 쓰는 작업을 새로 띄우지 말 것. 메모리가 부족해지면 양쪽 다 10배 느려진 전례가 있다.
- 데스크톱에서 시험 삼아 서버를 띄울 일이 있으면 8766~8769 를 피해 다른 포트(예: 8796)를 쓰고,
  **서버와 브리지 양쪽 모두** 그 포트로 지정한다. 브리지 기본값도 8766 이다.
- 시뮬레이션 코드(`lehome_scene.py`, 평가·수집 스크립트, 자동 체인)를 수정하지 말 것.
  실물 쪽에서 고칠 것이 생기면 `31_real_robot_bridge.py` 와 이 문서·순서서 범위에서 해결한다.
- 모델을 다시 학습시키지 말 것. 학습은 시뮬 대화방 담당이다.

실물 쪽에서 시뮬 코드의 버그를 발견하면 고치지 말고 **기록만 해서** 시뮬 대화방에 전달한다.

---

## 16. 참고: 시뮬레이션 성적 (2026-09-07 기준)

| 모델 | 학습 시연 수 | 평가 횟수 | 성공률 |
|---|---|---|---|
| 우승자 대형 모델 | - | 120 | 85.8% |
| **소형 (실물에 올릴 모델)** | 674회 | 240 | **77.1%** |
| 소형 | 505회 | 240 | 73.3% |
| 소형 | 267회 | 120 | 58.3% |
| 소형 | 169회 | 120 | 40.0% |

- 시연 데이터 양이 성능을 좌우한다는 것이 지금까지의 가장 큰 결론이다.
- 단 이 표의 행들은 학습 데이터 수집 조건이 완전히 같지는 않다. 169회 모델만 환경 버그를 고친 뒤
  수집한 데이터이고, 674회 모델은 고치기 전 505회와 고친 뒤 169회를 합친 것이다.
  적은 양의 정확한 데이터가 많은 양의 부정확한 데이터에 졌다는 점이 데이터 양 주장의 근거다.
- 평가 오차는 120회 기준 약 ±8%p, 240회 기준 약 ±6%p 다. 모델 간 3~5%p 차이는 의미가 없다.
