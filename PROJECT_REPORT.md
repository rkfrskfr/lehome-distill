# LeHome 옷 접기 챌린지 — 경량 정책 복제 프로젝트 보고서

- 작성일: 2026-08-28
- 작업 환경: Windows 11 / NVIDIA RTX 5090 32GB 1대 / Isaac Sim 5.1 (물리 시뮬레이터) + WSL2

---

## 1. 프로젝트 한 줄 요약

ICRA 2026 LeHome 챌린지(양팔 로봇 옷 접기 대회) **우승자가 공개한 대형 AI 모델(28.3억
파라미터)의 행동을, 그 약 1/55 크기(5,170만 파라미터)의 소형 모델에 학습시켜 복제하는
프로젝트.** 현재 소형 모델의 성공률 **47.5%** (옷 12벌 × 10회 = 120회 시도) 달성 —
동일 조건에서 측정한 우승자 모델(46~50%)과 동급 수준.

| 단계 | 성공률 (120회 기준) |
|---|---|
| 초기 소형 모델 (시연 59회로 학습) | 28.0% (100회 측정) |
| 데이터 증량·다양화 실험 3종 | 25.0~30.8% |
| **추론 방식 수정 + 이미지 증강 (현재 최고)** | **47.5%** |
| 우승자 모델 (동일 시뮬레이터에서 측정) | 45.8% (120회) ~ 50% (100회) |

---

## 2. LeHome 챌린지 소개

- ICRA 2026(로봇 분야 최대 학회) 공식 대회. 옷·수건 같은 변형 물체 조작이 주제
  (lehome-challenge.com).
- **과제**: 테이블 위에 놓인 옷 한 벌을 **양팔 로봇(SO-ARM101, 6자유도 팔 2개 =
  12개 관절)** 으로 접는 것.
  - 입력(관측): RGB 카메라 3대 영상 — 위에서 내려다보는 카메라 1대 + 양쪽 손목 카메라 2대
  - 출력(행동): 12개 관절의 목표 각도, 초당 30회
- **성공 판정**: 옷 위의 기준점 쌍 사이 거리 조건(긴팔 상의 기준 5개 — 소매 접힘 2,
  몸통 접힘 1, 어깨·밑단 벌어짐 유지 2)을 **전부** 만족해야 성공. 부분 점수 없음.
  1회 시도당 최대 600스텝(약 20초) 제한.
- **옷 구성**: 4종류(긴팔 상의 / 반팔 상의 / 긴바지 / 반바지) × 종류당 20벌 = 80벌.
  종류당 10벌은 학습용 시연 데이터가 공개된 옷(Seen), 10벌은 학습 데이터가 전혀 없는
  처음 보는 옷(Unseen) — 일반화 능력까지 평가하는 구조.
- **2개 라운드**: ① 온라인 라운드 — Isaac Sim 시뮬레이터에서 채점, 공개 리더보드
  ② 오프라인 결선 — 상위 8팀이 비엔나 현장에서 **실물 로봇**으로 평가
  (시뮬레이터에서 학습한 정책의 현실 이전 능력까지 검증).

---

## 3. 우승자 솔루션 분석

Ilia Larchenko, *"Learning to Fold"* (arXiv:2606.27163).
**온라인 라운드 62팀 중 1위(79.63%), 실기계 결선 2위(1080점 만점에 865점).**
코드와 학습 완료 모델 전체 공개 (github.com/IliaLarchenko/lehome_solution).

주요 구성:

1. **모델**: π0.5 계열 VLA(Vision-Language-Action — 이미지를 보고 로봇 행동을 출력하는
   대형 모델). 이미지 인코더(SigLIP) + 20억 파라미터 트랜스포머 + 3억 파라미터 행동
   생성기. 총 **28.3억 파라미터**. 한 번의 추론으로 30스텝 분량의 행동 묶음(청크)을 출력.
2. **학습 방법**: 시연 따라하기(행동 복제)로 시작한 뒤 **강화학습으로 마무리** —
   시뮬레이터에서 총 12,500회를 스스로 시도하며, 성공에 기여한 행동을 더 자주
   학습하는 방식(AWR·RECAP 계열). H200 GPU 1대에서 30만 스텝 학습.
3. **모델이 자기 성적을 스스로 예측**: 행동 외에 성공확률·진행도·옷 종류 등을 함께
   출력하는 보조 예측기를 내장. 이 예측값을 학습 신호 계산, 실패 조기 감지,
   후보 행동 선택에 재사용.
4. **데이터 수집 인프라** (성능의 실질 원천):
   - GPU 1대당 시뮬레이터 3~5개 병렬 실행
   - 가망 없는 시도 조기 중단, 멈춘 시뮬레이터 자동 재시작
   - **물리 상태 저장·복원**: 성공한 시도의 초기 상태를 저장해 두고, 화면 색·질감만
     바꿔 다시 실행 → 희귀한 성공 사례를 여러 배로 증식. 실패 직전 상태를 저장해
     그 지점부터 재시도 → 어려운 상황 집중 훈련
   - 못 접는 옷일수록 더 자주 수집하는 커리큘럼
   - 강한 시각 랜덤화(옷·테이블 무늬 교체, 조명·카메라 흔들기)
5. **추론 시 최적화**: 30스텝 행동 묶음 중 **앞의 5스텝만 실행하고 즉시 다시 관측·재계획**.
   후보 행동 2~3개를 뽑아 예측 성공확률이 높은 것을 고르는 best-of-N 등.
6. **저자의 자체 평가**: "이 과제에 이 모델 크기는 과했다. 실용 환경이라면 훨씬 작은
   모델로 시작했을 것" / "실수한 뒤 복구하는 능력은 미해결 과제로 남음."

---

## 4. 구축한 실험 환경 (윈도우 복제 시뮬레이터)

공식 환경은 Ubuntu 전용이라, **Windows용 Isaac Sim 5.1 위에 대회 환경을 직접 복제**함.

```
Isaac Sim (시뮬레이터, Windows)
   ├─ TCP → 중계 프로그램 → WebSocket → 우승자 모델 서버 (WSL2 리눅스, JAX)
   └─ TCP → 소형 모델 서버 (PyTorch/LeRobot)
```

- 공식 씬·옷 에셋·성공 판정 코드를 이식. 이 과정에서 좌표 미적용, 각도 단위 오류
  (57배 폭발), 로봇 높이 2.2cm 오차, 판정 기준점 인덱스 밀림 등 **버그 16개를 실측으로
  해결**. 카메라 3대는 공식 학습 데이터와 픽셀 단위 정렬 확인(오차 1~2%).
- **환경 검증**: 우승자 공개 모델을 이 복제 환경에서 돌리면 46~50%(120회).
  우승자 보고 수치(공식 서버·긴팔 74.5%)와 차이가 있으나, 이는 시뮬레이터 미세 차이와
  추론 튜닝 유무 때문 — **같은 환경 안에서의 상대 비교는 유효**하다는 것이 핵심.
- 미해결 사항: 공식 물리 주기(1/90초)가 이 환경에서는 동작하지 않아 1/60초 사용.
  따라서 모든 수치는 "복제 환경 기준"으로 명시함.

---

## 5. 접근 방법: 대형 모델의 행동을 소형 모델로 복제 (지식 증류)

**우승자 모델을 그대로 쓰지 않는 이유**:
- 학습·수집·추론 코드는 공개돼 있으나, 사전학습 데이터(π0.5 기반 모델의 원천)와 우승자가
  사용한 연산 규모(H200, 배치 192, 롤아웃 12,500회)는 재현 불가 → 실질적으로 고칠 수 없는 모델
- 28.3억 파라미터는 대학원 자원으로 재학습 불가. 소형 모델은 2시간이면 재학습 가능
- 데이터 수집→학습→배포 전 과정을 직접 소유해야 실물 로봇 이전 시 대응 가능
- "작은 모델에 능력이 얼마나 담기는가, 어떤 데이터가 중요한가" 자체가 연구 주제

**구조**:
- **원본(시연자)**: 우승자 π0.5 모델. 학습하지 않고 고정한 채, 복제 환경에서 옷을
  접게 하여 **성공한 시도만 기록** (카메라 3대 영상 + 관절 상태 + 행동).
- **복제 대상**: ACT(Action Chunking Transformer, LeRobot 구현, 5,170만 파라미터).
  "이 화면이면 이렇게 움직인다"를 기록에서 지도학습. 한 번의 추론으로 100스텝
  행동 묶음을 출력하는 구조.
- 학습 설정: 6만 스텝, 배치 16, AdamW(학습률 1e-5), 이미지 증강 적용.

---

## 6. 데이터 수집 파이프라인

우승자 논문의 데이터 인프라를 순차 이식하여 수집기에 구현함:

| 기능 | 내용 |
|---|---|
| 옷별 균형 수집 | 옷마다 성공 N회를 채울 때까지 시도(상한 3N) — 쉬운 옷 쏠림 방지 |
| 도메인 랜덤화 | 로봇 높이 ±10mm, 옷 낙하 높이 가변, 조명, 테이블 무늬 100종, 옷 무늬 44종, 카메라 위치·초점 흔들기, 스텝 단위 조명 변화 |
| 물리 상태 저장 | 성공 시도의 초기 상태(증식용) + 실패 시도의 "망치기 시작한 순간" (원본 모델의 성공확률 예측이 최고치 대비 0.12 하락하는 시점, 집중 재시도용) |
| 실패 시도 보관 | 성공확률 예측기 학습 등의 원료 (실패 608회 확보) |
| 조기 중단 | 180스텝간 판정 조건에 진전이 없으면 폐기 — 수집 속도 약 30% 향상 (실측) |
| 예측값 기록 | 원본 모델이 매 스텝 출력하는 성공확률·진행도를 프레임별 저장 |

**수집 누적** (긴팔, 당시 판정기 버그로 통과 불가였던 Seen_8 제외 9벌 — 7.2절 참고):

| 배치 | 성공 시도 | 조건 |
|---|---|---|
| 1차 | 200 | 랜덤화 없음 |
| 2차 | 137 | 높이 ±5mm + 조명 |
| 3차 | 130 | 높이 ±10mm + 낙하 + 조명 + 무늬(절반) |
| 4차 | 241+ | 3차 동일 + 상태 저장·실패 보관·조기 중단 |
| **합계** | **약 700회** | + 실패 608회 + 물리 상태 저장본 약 5,000개 |

---

## 7. 실험 결과와 핵심 발견

### 7.1 데이터만으로는 정체 (25~31%)

| 모델 | 학습 데이터 | 전체 | Seen | Unseen |
|---|---|---|---|---|
| 1호 | 59회 | 28.0% | 30% | 20% |
| 2호 (증량) | 195회 | 30.8% | 37% | 0% |
| 3호 (랜덤화) | 137회 | 25.0% | 27% | 15% |
| 4호 (혼합) | 267회 | 28.3% | 32% | 10% |

데이터를 4.5배로 늘려도 전체 성적은 오차범위 안. 단, 옷 단위로는 "시연이 많은 옷일수록
잘 접음"(40회분 옷 80% vs 8회분 옷 0%)이라는 상관은 뚜렷했음.

### 7.2 결정적 발견: 행동 묶음 소비 길이 ★

LeRobot의 ACT 기본 설정은 한 번의 추론으로 만든 **100스텝 행동 묶음(약 3.3초 분량)을
카메라를 다시 보지 않고 끝까지 실행**하는 방식. 우승자는 30스텝 묶음 중 앞의 5스텝만
실행하고 즉시 재관측·재계획(0.17초 주기). 같은 학습 완료 모델에서 소비 길이만 바꿔
비교한 결과 (옷 4벌 × 8회):

| 한 번에 실행하는 스텝 수 | 성공률 |
|---|---|
| 100 (기본값) | 31.2% |
| 20 | 62.5% |
| **5** | **65.6%** |
| 1 | 59.4% |

**재학습 없이 설정 하나로 성능 2배.** 천은 3초 앞을 예측할 수 없는 대상이라, 짧게
실행하고 자주 다시 보는 폐루프 제어가 결정적. 기존 모든 성적(25~31%)은 이 기본값
아래에서 측정된 수치였음.

이 설정으로 정식 평가(12벌 × 10회 = 120회): **전체 47.5%** (Seen 50%, Unseen 35%,
판정 불능 옷 제외 시 51.8%). 옷별 최고 100%(Seen_2), 90%(Seen_9), 80%(Seen_4).
성공 시 소요 시간 중앙값 약 5초(시뮬레이션 시간, 50스텝 단위 판정) — 원본 모델보다 빠름.

> ⚠ **판정기 수정 (2026-09-05 17:00)**: 위 수치를 포함해 이 시각 이전의 모든 성공률은
> "구판정" 기준. 원인 조사 결과 물리 엔진이 옷을 입자로 변환할 때 좌표가 위로 23cm
> 이동되는데, 이식한 판정기가 이를 보정하지 않고 가장 가까운 입자를 기준점으로 잡아
> **모든 옷의 기준점이 실제 위치와 어긋나 있었음**. "아무도 못 접는 옷" 으로 알려졌던
> Seen_8 은 이 때문에 어깨 기준점 거리가 4.6cm(기준 10.8cm)로 애초에 통과가 불가능했던
> 것. 입자 변환 규칙(같은 좌표 점은 첫 번째만 유지)을 12벌 전부에서 확인해 기준점을
> 정확히 복원하도록 수정했고, 현재 원본 모델과 소형 모델을 수정된 판정기로 재평가 중.
> 수정 후 수치는 구판정 수치와 직접 비교하지 않음.
>
> ⚠ **초기 자세 수정 (같은 날 20:35)**: 위 "23cm" 의 출처를 추적한 결과, 물리 초기화 시
> 옷이 테이블 내부에서 생성돼 밀려난 좌표를 기준 자세로 쓰고 있었고, 그 때문에 매 시도마다
> 옷을 테이블 위 34cm 에서 떨어뜨리고 있었음(공식은 1cm 위에서 시작). 공식보다 훨씬 구겨진
> 시작 상태였던 것으로, 공식 리더보드(긴팔 74.5%) 대비 우리 복제 환경 성공률이 낮았던 주요
> 원인 후보. 수정 후 원본·소형 모델을 다시 측정 중이며, 이전의 수집 데이터도 모두
> "높은 낙하" 조건에서 만들어진 것임.
> **검증**: 수정 후 원본 모델의 성공률이 12벌 120회에서 **85.8%**(103/120) — 공식 리더보드
> 긴팔 74.5% 와 같은 수준. 복제 환경이 공식과 정합함을 확인.
> **고친 환경 소형 모델 성적 (9/6, 12벌 × 10회)**: 시연 505회로 학습한 모델 **75.8%** (최고),
> 267회 모델 58.3%, 고친 환경에서 새로 수집한 169회만으로 학습한 모델 40.0%. 시작 상태를
> 정확히 맞춘 적은 데이터보다 데이터 양이 성능을 좌우. 원본(85.8%)과의 격차 10%p.

### 7.3 부가 발견

- **성패는 시작 순간에 대부분 결정됨**: 시도의 **첫 프레임 한 장**만으로 성공/실패를
  AUC 0.82로 예측 가능 (수집 조건 단위로 검증 데이터를 분리해 단순 암기 가능성 배제).
  옷이 테이블에 떨어진 모양이 성패를 지배하며, 나쁜 시작에서 복구하는 능력이 다음
  병목 — 우승자 논문의 "복구 미해결" 결론과 일치.
- **이미지 증강 효과**: 5스텝 실행 조건에서는 +6%p (59.4→65.6%)였으나 100스텝 실행 조건에서는
  -9%p (40.6→31.2%). 표본 32회씩이라 어느 쪽도 통계적 확정은 아니며, 증강 단독 효과는 미확정.
- **행동 앙상블(ACT 내장 기능)**: 효과 없음(37.5%).
- **원본 모델의 행동 주기 변경(33.3ms)**: 기존(16.7ms)보다 나쁨(59.4% vs 71.9%) —
  기존 설정 유지 확정.
- **학습률 사고 사례**: 우승자 논문의 학습률(1e-4)을 그대로 적용한 학습이 3.3%로 붕괴.
  논문은 배치 192 기준, 이쪽은 배치 16 — 배치 크기에 맞춘 학습률 조정이 필수라는 교훈.

---

## 8. 자동화: 무인 실험 루프

우승자의 비동기 학습/수집 루프를 GPU 1대 환경에 맞게 축약한 자동화 스크립트 구축:

```
수집(시뮬 2개 병렬, 커리큘럼) → 상태 복원 증식·집중 재시도 → 데이터 변환
→ 성공확률 예측기 학습 → 소형 모델 학습(증강 적용) → 평가(120회, 시뮬 오류 자동 재시도)
→ 판단(못하는 옷에 수집 재배분, 정체 시 이전 최고 모델로 복귀) → 반복
```

- 모든 진행 상태를 파일로 기록 — 중간에 죽어도 재실행하면 그 지점부터 재개
- 모델 서버가 죽으면 자동 재기동, 단계별 제한시간 감시
- 사람용 요약 파일 자동 갱신 (사이클별 성적, 옷별 성공률)

---

## 9. 현재 상태와 향후 계획

**완료된 후속 실험 (2026-09-05, 구판정)**: 700회 전량 학습 30.8%, 대형 모델(파라미터 1.5배) 44.2% —
둘 다 267회 모델(47.5%)을 넘지 못함. 랜덤화 없는 초기 200회가 섞인 데이터 혼합이 원인으로
추정되어, 랜덤화 일관 데이터(505회)만으로 재학습 진행 중.

**판정기 수정 (2026-09-05 17:00, 7.2절)**: 기준점 매핑 버그를 고친 뒤 원본 모델·최고 모델·
증강 제거 모델·선별 데이터 모델을 같은 조건으로 재평가 중. 이후 보고 수치는 전부 신판정 기준.

**다음 단계** (우선순위 순):
1. 700회 학습 모델 평가 — 47.5% 초과 여부 확인, 목표 60%대
2. 성공 기여도 가중 학습 — 원본 모델이 남긴 프레임별 성공확률을 학습 가중치로 활용
   (우승자의 강화학습을 지도학습 틀 안에서 근사)
3. 집중 재시도 데이터 투입 — 실패 직전 상태에서 원본 모델이 복구에 성공한 시연
   = "복구하는 법" 데이터. 우승자도 시뮬레이터에서는 확보하지 못한 종류
4. 모델 크기 확대 / 입력 해상도 조정 비교
5. 장기: 환경 변화(로봇 높이 등)에 대한 강건성 정량화 → 실물 SO-101 로봇 이전

**한계 (명시 사항)**:
- 순수 모방학습의 이론적 상한은 원본 모델 수준(46~50%) — 이를 넘으려면 위 2·3번
  같은 추가 기법 필요
- 물리 주기가 공식과 달라(1/60 vs 1/90초) 모든 수치는 "복제 환경 기준"
- 120회 전체 성공률의 95% 신뢰구간은 약 ±9%p, 옷별(10회) 수치는 ±30%p 수준으로 노이즈가 큼 —
  옷별 비교는 참고용, 중요 비교는 판수를 늘려 수행

---

## 부록 A. 주요 산출물

| 파일 | 내용 |
|---|---|
| `lehome_scene.py` | 복제 환경 (물리·카메라·판정기·랜덤화) |
| `16_collect_distill.py` | 시연 수집기 (상태 저장·실패 보관·조기 중단) |
| `22_replay_snapshots.py` | 상태 복원 증식 / 집중 재시도 |
| `23_train_success_head.py` | 성공확률 예측기 |
| `orchestrator.py` | 무인 실험 루프 |
| `12_policy_server.py` | 소형 모델 추론 서버 (행동 소비 길이 조절 포함) |
| `bench_*.csv` | 전체 평가 원자료 (최고 기록: `bench_final_n5.csv`) |
| `LEHOME_paper_notes.md` / `AUDIT.md` | 우승자 논문 분석 노트 · 구현 점검표 |

## 부록 B. 수치 출처

본문의 모든 성공률은 복제 환경에서 실측한 CSV 기록 기준. 초기 모델들(각 120회),
추론 설정 비교(옷 4벌 × 8회), 최고 기록(120회, `bench_final_n5.csv`),
원본 모델 측정치 포함. 우승자 공식 수치는 arXiv:2606.27163 §8 인용.
