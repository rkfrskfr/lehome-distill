# 논문 대비 전수 점검 (2026-08-26)

arXiv:2606.27163 의 모든 섹션을 우리 코드와 1:1 대조. **코드로 확인한 것만** 적었다.

## A. 이번에 고친 것 (전부 검증 완료)

| # | 항목 | 무엇이 문제였나 | 조치 |
|---|---|---|---|
| 1 | 교사 성공확률 폐기 | 우승자 모델이 매 스텝 `success_pred` 를 보내고 중계기가 받고 있었는데 수집기가 **버리고 있었다** | `preds.npy` 기록 + 논문 §3.2 규칙(EMA 최고치 대비 0.12 하락 = '망친 순간')으로 스냅샷 |
| 2 | 스냅샷이 무의미한 지점 | 100스텝마다 기계적으로 저장 → 하드마이닝 가치 낮음 | 위 규칙으로 교체 (step 5 성공상태는 유지) |
| 3 | 카메라 지터 없음 | 논문 §3.3 episode-level 증강 중 카메라 항목 누락. §9.1 은 렌더 세부 과적합을 직접 경고 | `jitter_cameras()` (±5mm/±1°/±3% 초점), `LEHOME_RAND_CAM=1` |
| 4 | 스텝 단위 증강 없음 | 에피소드 단위만 하면 "이 판은 이런 색" 고정 신호가 남음 (논문 §3.3 per-step) | `perstep_light()` 10스텝마다, `LEHOME_RAND_PERSTEP=1` |
| 5 | 학습 레시피 | 논문 §2.6 레시피(코사인 1e-4, 배치 48, AMP)를 시도했으나 **배치 16 에서 1e-4 는 붕괴(3.3%)·배치 48 은 VRAM 초과** — 검증된 레시피(프리셋 1e-5, 배치 16)로 회귀 | 롤백 완료 |
| 6 | **인자가 조용히 무시됨** | `use_policy_training_preset` 기본 true 라 optimizer/scheduler CLI 인자를 **덮어씀** — 그대로 뒀으면 "적용했다"고 착각할 뻔 | false 로 끄고 `26_check_train_args.py` 로 파싱 결과 실측 (PASS) |
| 7 | 하드마이닝 미연결 | `22_replay_snapshots.py --mode recover` 를 만들어놓고 자동 루프에 안 붙임 | orchestrator `hard_mine()` 단계 추가 |

## B. 아직 안 한 것 (우선순위 순)

### B1. ✅ 완료 — 실측 결과: 청크 5 실행 65.6% vs 100 실행 31.2% (2배), 시간앙상블 37.5%(무효), 교사 33.3ms 59.4%(기존 16.7ms 71.9% 유지)
- **행동 청크 소비 길이 (`n_action_steps`)**: 우리 학생은 `chunk=100, n_action_steps=100`
  → **100개 행동(약 3.3초)을 카메라를 다시 보지 않고 그대로 실행**한다.
  논문 §7.1/표4 는 30개 중 **5개만 실행하고 재계획**(0.17초)이 최적이라 실측 —
  "가까운 스텝은 훨씬 정확하니 신선한 관측으로 자주 재계획하는 게 이득".
  → 재학습 없이 서버 옵션(`--n-action-steps`)만으로 시험 가능. **20배 차이**.
- **시간 앙상블 (`temporal_ensemble_coeff`)**: ACT 고유 기능, 우리는 꺼둠(None).
  논문의 soft inpainting/부드러운 궤적에 대응. 서버 `--ensemble` 로 시험 가능.

### B2. 🟠 큰 이득, 중간 공수
- **병렬 심 2대**: 논문은 3~5대(§3.1). 우리는 1대 → 수집 속도 2배 여지 (VRAM 상 2대가 한계).
- **AWR 가중 샘플링**: lerobot 에 `sample_weighting`(RA-BC) 훅이 이미 있음.
  이제 프레임별 교사 `success_pred` 가 있으니 진행도 기반 가중치를 만들 수 있다.
- **학생용 성공 헤드 → best-of-N**: ACT 에 헤드를 붙여야 함. 논문 §7.4 는 N=2~3 에서
  일관된 이득("최악 회피"). 우리 자체 예측기는 이미지 전용이라 행동 후보를 못 고른다.
- **per-timestep 행동 정규화** (§4.6): 청크 뒤쪽 분산이 √t 로 커지는 문제.
  lerobot ACT 는 전역 정규화.

### B3. 🟡 있으면 좋음
- 소스별 샘플링 비율·감쇠 (§2.4): 지금은 전 데이터 균등 병합.
- semi-success 카테고리 (첫 체크포인트 도달 후 실패) 를 별도 리플레이 대상으로.
- 디버그 영상에 성공확률/진행도 오버레이 (§3.1 그림4) — 사람이 보기 좋음.
- LAB 색 리맵, 옷 scale/roughness 흔들기, 돔라이트 회전 (§3.3 나머지 항목).
- Thompson 샘플링으로 옷별 추론 파라미터 자동 튜닝 (§7.7).

### B4. ⚪ 우리 상황에 부적합/보류
- π0.5 구조 개조(XSA, KV캐시 믹싱, FAST 토큰, CFG) — flow-matching VLA 전용.
- 실제 RL(AWR+RECAP 전체) — 롤아웃 12,500판 규모, 우리 1대로는 비현실적.
- sim2real 도구(카메라 오버레이 정렬 등) — 실기계 단계에서.

## C. 평가 프로토콜에서 개선할 점
- 옷당 10판 → 오차 ±15%p. 자동 판단(커리큘럼·롤백)이 노이즈를 쫓을 수 있다.
  중요한 비교는 15~20판으로.
- ~~평가에 Seen_8(교사도 0%, 성공조건 상충) 이 포함돼 전체 평균을 끌어내린다.~~
  → **2026-09-05 원인 규명: 우리 판정기 버그였다** (아래 E 항 참조). Seen_8 은 정상 판정
  가능해졌고, 다른 옷의 기준점도 함께 바로잡혔다. 09-05 17:00 이전 수치는 전부
  "구판정 기준" 으로 표기하고, 신판정 수치와 섞어 비교하지 말 것.

## D. 논문 밖 — SO-101/LeRobot 일반 관점에서 추가로 찾은 것 (2026-08-26)

| 우선 | 항목 | 근거 / 현재 상태 |
|---|---|---|
| 🔴 | **교사 행동 주기 33.3ms 미시험** | 논문은 "sim 에서 30Hz". 우리 교사는 16.7ms(=60Hz)로 굴린다. 시험한 점은 11.1ms(나쁨)·16.7ms(최적)·50ms(나쁨) 뿐 — **논문 실제값인 33.3ms 만 빠졌다.** 벤치 1회로 확인 가능. 맞으면 교사 성공률↑ = 수집 효율↑ |
| 🔴 | **입력 해상도 480×640 그대로** | 우승자는 224×224 로 리사이즈. 우리는 카메라당 6.1배 픽셀 → 학습 3~5배 느리고 렌더 세부 과적합 위험(§9.1). 리사이즈 학습 1회 비교 가치 |
| 🟠 | **체크포인트 선택이 항상 마지막** | 20k/40k/60k 중 최고를 고르지 않음. 과적합 구간이면 손해 |
| 🟠 | **PhysX attachShape 플레이크 자동 재시도 없음** | 평가에서 옷이 통째로 누락될 수 있는데(HANDOFF §5-2) 자동 루프에 재시도 미구현. CSV 행수 검사 후 누락분 재실행 필요 |
| 🟡 | **학습 중 검증 분할 없음** (`eval_split=0.0`) | 과적합 시점을 못 본다 |
| 🟡 | **학습 시드 1개(1000)** | 학생 간 차이(25~31%)가 시드 분산일 가능성 미배제 |
| 🟡 | **그리퍼 물리재질 없음** (PhysX 기본 0.5) | 실물 SO-101 은 마찰 큰 패드. 천 파지 성공률에 직결되나 **공식 이탈**이라 대외 수치는 공식 기준 유지 필요 |
| ⚪ | 관측 이력 1프레임 (`n_obs_steps=1`) | 천이 잡혔는지 여부가 한 장으론 모호. ACT 는 다중 프레임 미지원이라 구조 변경 필요 |

디스크 여유 799GB (수집 계속 가능).

## E. 2026-09-05 멀티에이전트 전수 감사 (64건 → 검증 후 49건 확정) — 반영 현황

| 상태 | 항목 |
|---|---|
| ✅ 수정 | 병렬 심 2 → 1 (중계기 단일 클라이언트), 학습 워치독 14h·배치 16, 스윕 `$g/$G` 변수 충돌(스윕 붕괴 원인), 오케스트레이터 재개(FileExistsError)·프리셋 레시피 복귀·커리큘럼 정렬·회복 소스 포함·pid 정규식·예측기 청크 분할 |
| ✅ 수정 | 평가 프로세스 LEHOME_RAND_* 환경변수 자동 제거, CSV 고정 필드(PLACE_FAIL 첫 행), 서버 자동 리사이즈(저해상도 모델)·합의 큐 초기화·`--port` |
| ✅ 수정 | 리플레이: 스냅샷 robot_z 정합(다수 z 선택), 에피소드별 분산 선택, 오프라인 판정점수 선별(거의 성공 우선), 옷텍스처 env 게이팅, 사이클별 출력명 |
| ✅ 수정 | 씬: per-step 조명 기준값 주변 지터, 옷 재질 지터 UsdPreviewSurface 입력명 |
| ✅ 수정 | 주말 체인 v2: 환경변수 정리, 부족분만 재실행, 서버 포트 대기, 회복데이터 20판 미만 시 D 생략, Seen_9 제외, chunk30 실험 추가 |
| ✅ 문서 | 1/55, 교사 45.8~50%(n 명시), 28.0%(n=100), 증강 효과 양면 기재, CI 문구, 학습 인프라 "공개" 정정, 예측기 누수 명시, 진행 중→완료 |
| ✅ 수정 | **성공 판정기 기준점 매핑 버그** (`32_judge_probe`, `33_dup_check`): PhysX 쿠킹 입자 좌표가 메시 좌표 대비 **z +23cm(+수 도 기울기)** 이동돼 있는데 정렬 없이 최근접 매핑을 해서 20cm 떨어진 엉뚱한 입자를 기준점으로 잡고 있었다. Seen_8 은 어깨 거리가 4.6cm(임계 10.8)로 구조적 통과 불가, 나머지 옷도 기준점 오염. 쿠커 용접 규칙("같은 좌표 점은 첫 등장만 유지, 순서 보존")을 12벌 전부에서 확인(중복 수 == 사라진 점 수)해 **정확한 인덱스 대응**으로 교체(v3), 실패 시 ICP 정렬 후 최근접. 용접 옷 3벌 검증: 매핑 후 기준점 거리가 메시 기준과 0.5cm 이내 일치 |
| ✅ 확인 | **공식 판정기 의미와의 일치** (WSL `lehome-challenge/source/lehome/lehome/utils/success_checker_chanllege.py`, `assets/object/Garment.py`): 공식은 GPU 경로에서 `cloth_prim_view.get_world_positions()[check_point]` 로 입자 배열을 **그대로** 인덱싱하고, 초기화 시 메시 `points` 속성을 입자 좌표로 덮어쓴다(=점 수가 같다는 전제, 즉 공식 쿠킹은 용접하지 않음). 따라서 공식 기준점 = 메시 순서의 점. 우리 Windows 5.1 쿠커는 중복 점을 용접하므로 v3 의 "용접 보정 후 메시 인덱스" 가 공식 의미를 정확히 재현한다. **잔여 불확실성**: 공식 환경(리눅스 Isaac Sim)도 용접한다면 공식은 "밀린 인덱스"를 그대로 쓰는 셈인데, `33_dup_check` 결과 두 해석이 갈리는 옷은 **Seen_4 하나**(어깨 점 0·1 이 32 밀림 → 그 해석에선 어깨 거리 0.5cm 로 통과 불가). 나머지 11벌은 체크포인트 앞에 중복 점이 없어 두 해석이 동일 |
| ⏳ 진행 | 신판정 기준 재평가: 원본 모델(`teacher_v3`, teacher_eval.ps1 8767) → Seen_8 첫 수집(seen8_chain.ps1) → 챔피언 재현(`final_n5_rep`)·증강 제거(`noaug_full_n5`) (post_eval.ps1 8768, 다른 Isaac 평가가 없을 때만 실행), 선별 데이터 모델(`cur_n5`), 주말 체인(seed2/chunk30/cur2/cur240) — 전부 신판정 |
| ✅ 수정 | **초기 자세 버그 — 옷을 34cm 높이에서 떨어뜨리고 있었다** (`35_frame_probe`, 09-05 20:35): 판정기 버그의 "+23cm" 정체 추적 결과, `world.reset()` 직후 쿠킹 입자 좌표는 휴지 자세가 아니라 **옷이 원점(테이블 충돌체 내부)에서 밀려난 상태**(중심 z +0.23, 7° 기울기, 3cm 비강체 변형)였다. 이걸 매 판의 기준 자세로 써서 `rest + (0,0,0.63)` → 옷 중심 0.86m(상판 위 34cm)에서 낙하. 공식은 프림을 0.63 에 놓고 쿠킹하므로 옷 하단이 상판 1cm 위에서 시작(거의 얹어 놓는 수준). 수정: 휴지 자세 = 메시 점 × scale(용접 순서 정합). 수정 후 배치 중심 0.630·하단 0.556, 정착 후 옷 x 폭 0.493→0.538 로 더 평평. **20:35 이전의 모든 수집 데이터·평가는 "높은 낙하(구겨진 시작)" 조건** — 공식 대비 성공률이 낮았던 주요 원인 후보 |
| ✅ 수정 | **GPU 과적재** (09-05 저녁): 학습 + Isaac 평가 2개 + 교사 JAX 서버 + 서버 2개 = VRAM 30.4/32.6GB → 페이징으로 전 작업 10배 지연(평가 1판 2013초). 평가 병렬 1개 원칙으로 재편성(ablation.ps1 중단 → post_eval.ps1) |
| ⚪ 보류 | 돔라이트 회전(무텍스처라 무효), 그리퍼 마찰(미사용), 60k 고정 스텝의 데이터량 교란(에폭 기준 비교는 후속) |

무효 판정된 과거 수치: `bench_aug_n10 / aug_n5_cons3 / teach_n5` (스윕 붕괴로 마지막 옷·잘못된 시드만 실행).
